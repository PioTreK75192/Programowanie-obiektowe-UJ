<!doctype html>
<html lang="pl">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Kategorie</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <nav class="navbar navbar-expand bg-white border-bottom">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/">Magazyn</a>
            <div class="navbar-nav">
                <a class="nav-link" href="/products">Produkty</a>
                <a class="nav-link active" href="/categories">Kategorie</a>
                <a class="nav-link" href="/suppliers">Dostawcy</a>
            </div>
        </div>
    </nav>

    <main class="container py-4">
        <section class="d-flex align-items-center justify-content-between gap-3 mb-4">
            <div>
                <p class="text-uppercase text-primary fw-semibold small mb-1">CRUD</p>
                <h1 class="h2 mb-0">Kategorie</h1>
            </div>
            <a class="btn btn-primary" href="/categories/new">Nowa kategoria</a>
        </section>

        <section class="table-responsive bg-white border rounded">
            <table class="table table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th>Nazwa</th>
                        <th>Opis</th>
                        <th>Produkty</th>
                        <th>Akcje</th>
                    </tr>
                </thead>
                <tbody>
                    #for(category in categories):
                    <tr>
                        <td><strong>#(category.name)</strong></td>
                        <td>#(category.description)</td>
                        <td>#(category.productsCount)</td>
                        <td>
                            <div class="d-flex flex-wrap gap-2">
                                <a class="btn btn-sm btn-outline-secondary" href="/categories/#(category.id)/edit">Edytuj</a>
                                <form method="post" action="/categories/#(category.id)/delete">
                                    <button class="btn btn-sm btn-outline-danger" type="submit">Usun</button>
                                </form>
                            </div>
                        </td>
                    </tr>
                    #endfor
                </tbody>
            </table>
        </section>
    </main>
</body>
</html>
